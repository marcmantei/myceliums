# Real Heading

This is normal prose.

```rust
# This line looks like a heading but it is inside a code fence.
fn example() {
    // not a heading
}
```

## Also a Real Heading

Back to prose after the fence.
