This markdown file has no headings at all.

It is just regular prose content spread across
multiple paragraphs without any heading structure.

The parser should emit one Document symbol and zero Section symbols.
